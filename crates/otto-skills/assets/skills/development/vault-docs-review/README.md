# vault-docs-review

Default read-only reviewer for repository documentation produced by an Otto
Vault scan. The runtime stages this entire package and expects the exact JSON
array in `references/finding-contract.md`.
