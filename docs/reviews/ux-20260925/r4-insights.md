# Round 4 final — reviewer 9/10: Assistant, History, Usage, Insights

Worktree only `/Users/itziklavon/otto-ux-audit-20260925`; no commits/subagents, no original checkout edits. Services and screenshots use synthetic fixtures. Prior real History read/resume uses the harness temporary transcript and fake CLI. No remote or provider-home mutations.

## Independently rechecked

`desktop-ux-insights.spec.ts desktop-ux-r2-insights.spec.ts desktop-ux-r3-insights.spec.ts desktop-insights.spec.ts`: **35 passed (1.9m)** in `/tmp/otto-ux-r4-insights-prior`, log `/tmp/otto-ux-r4-insights-prior.log`. Includes all earlier draft/focus/approval/deep-link/report-identity/Markdown/index/History filtering/Usage save races and populated real report rendering checks. R1–R3 screenshot filenames were regenerated by this run; inspected actual images, not prior reports' claims.

## Confirmed repairs

- P1: Insights request acceptance and collector execution used different clocks. Deterministic Python test reproduces wrong daily, weekly, monthly windows when collection begins after the next boundary. `crates/otto-server/src/insights.rs:697` captures acceptance date once, propagates `--as-of`, and reuses the scheduler's captured UTC due period for its completion event. Bundled collector supports the optional anchor without changing cadence or explicit adhoc range behavior. A SHA256-addressed collector is atomically materialized under daemon-owned `insights/collectors`, preserving installed v2/custom skills and their report instructions. Existing contents are verified; unexpected modified files are rejected, not overwritten. Parent granted Rust ownership and runs Rust gates/rebuild.
- P1: Manual Run now inherited scheduler idempotency instructions and stopped when the period already existed, while the UI waited for a changed report. With parent approval, `crates/otto-server/src/insights.rs:411` (`RunMode::Manual`) explicitly includes `--force` and regeneration instructions; `RunMode::Scheduled` retains catch-up idempotency. Rust regression checks both prompt contracts; no API request shape changes.
- P2: `ui/src/modules/assistant/AssistantComposer.svelte:81` displayed only the last failed filename in a multiple-file selection. Accumulates each failure while retaining successful attachments. Regression covers first+last failures surrounding a successful upload, then attachment-only send failure/retry.
- P2: `ui/src/modules/usage/UsagePage.svelte:58` permitted multiple positive caps for the same provider/workspace, yielding conflicting budget rows. Inline validation explains one cap per scope and disables Save until duplicates are removed.
- P1: `ui/src/lib/api/usage.svelte.ts:272` allowed a background GET begun during PUT to restore stale budgets after save success. Browser red received 30 instead of the saved 7. Request sequence guards cover loads before/during the mutation and their errors; local newer draft protection stays intact.
- P2: `ui/src/modules/insights/InsightsPage.svelte:403` enabled Run now before the initial report list resolved, leaving generation's existing-report fingerprint empty and allowing an unchanged report to appear newly completed. Run controls and command handler now wait for a successful baseline. Browser red confirms enabled during delayed baseline; regression expects disabled until baseline resolves.
- P3 composition: `ui/src/modules/insights/ReportDetail.svelte:421` KPI auto-fill reserved an unused desktop grid cell and formed an uneven three-plus-one tablet row. Auto-fit fills available desktop width; detail-container queries create two columns on narrow panes. No new tokens or guard debt.

## Files

`crates/otto-server/src/insights.rs`; `crates/otto-skills/assets/skills/insights/insights/{SKILL.md,scripts/collect_insights.py,scripts/test_collect_window.py}`; `.github/workflows/ci.yml`; `docs/contracts/api.md`; `ui/src/lib/api/usage.svelte.ts`; `ui/src/modules/assistant/AssistantComposer.svelte`; `ui/src/modules/usage/UsagePage.svelte`; `ui/src/modules/insights/{InsightsPage,ReportDetail}.svelte`; `ui/e2e/desktop-ux-r4-insights.spec.ts`.

## Validation details

All browser commands from worktree `ui/`, environment `OTTO_E2E_SLOT=ux4insights OTTO_E2E_PORT=7869 OTTO_E2E_PW_PORT=5369 OTTO_E2E_SWEEP_ORPHANS=0 OTTO_E2E_BIN=/Users/itziklavon/otto-ux-audit-20260925/target/debug/ottod`; `npx playwright test <specs> --project=desktop-browser --workers=1 --output=<path>`.

- Calendar red `/tmp/otto-ux-r4-insights-calendar-red.log`: three expected subtest failures (day/week/month). Green `/tmp/otto-ux-r4-insights-calendar-green.log`: 2 tests passed, includes three rollover cases and explicit adhoc-range compatibility. CI now runs `python3 -B -m unittest discover -s crates/otto-skills/assets/skills/insights/insights/scripts -p test_collect_window.py`, locally passed in `...-calendar-ci.log`.
- R4 red `/tmp/otto-ux-r4-insights-red.log`: duplicate-cap and missing-first-upload-name failures; initial cold navigation timed out before product assertion. Health bundle error/retry passed.
- R4 second `/tmp/otto-ux-r4-insights-second.log`: budget autorefresh reproduced saved7→30 rollback; upload-name repair passed but fixture Send locator ambiguous (approval and composer), corrected to composer. Initial cold navigation timeout corrected by warming on Memory then hash navigation. No production navigation logic changed to accommodate fixture.
- R4 deeper `/tmp/otto-ux-r4-insights-deeper.log`: 5 passed, 4 failed. Baseline-gating product red (Run enabled while list pending) fixed. History pagination focus automatically loads earlier before Enter; test now verifies that documented behavior. Cancellation fixture originally navigated to Tasks where generic cards are absent; it now exercises the real Chat task card. Scope-switch fixture lacked an actual workspace; seeded a disposable one. Timeout/retry and stale budget fix passed.
- E2E TypeScript `npx tsc --noEmit -p tsconfig.e2e.json`: exit0, `/tmp/otto-ux-r4-insights-tsc-final.log`.
- `node scripts/ui-guards.mjs`: exit0, `/tmp/otto-ux-r4-insights-guards.log`; no baseline increases.
- Scoped `git diff --check`: exit0.
- One Rust build found an unmapped io::Error in new collector preparation; corrected to otto_core::Error::Internal and parent notified for rerun. Parent owns final workspace Rust/Clippy/build/check evidence.

## Rendered evidence actually inspected

Freshly regenerated `/tmp/otto-ux-screenshots/insights-r2-reports-{native-light-1440,native-dark-1440,warm-light-390,warm-dark-1024,pro-dark-dark-1440}.png`; `insights-r1-assistant-light-loaded.png`; `insights-r1-tasks-dark-loaded.png`; `insights-r3-attachment-phone.png`; `insights-r3-usage-rtl-tablet.png`; `insights-r2-health-phone.png`. These contain synthetic fixtures. Final adjusted-layout and long-history captures pending below.

## Final verification, calibrated scores and limits

**Final desktop:** `desktop-ux-r4-insights.spec.ts desktop-ux-r3-insights.spec.ts desktop-ux-r2-insights.spec.ts`, output `/tmp/otto-ux-r4-insights-final`, log same stem `.log`: **36 passed (5.1m), exit0**. `.last-run.json` status passed, no failed tests. New suite contributes 12 tests; all baseline identity, duplicate scope, partial uploads + attachment-only send, bundle retry, budget auto-refresh, long history, Assistant live update/cancel, History scope switch, timeout/retry and three layout variants passed.

**WebKit iPhone:** same three spec files, `--project=iphone-portrait --grep 'attachment-only|support bundle failure|incremental turns|phone attachment recovery|phone report deep link|phone approval exposes' --workers=1 --output=/tmp/otto-ux-r4-insights-webkit`: **6 passed (32.4s), exit0**; same-stem `.log`, `.last-run.json` passed with no failures. All services synthetic. This includes full payload review, provider pin retry, multi-file recovery, live event updates/cancel and download recovery in actual Playwright WebKit.

Final screenshot evidence viewed:
- `/tmp/otto-ux-r4-insights-final/desktop-ux-r4-insights-Ins-469fe-oherently-native-light-1440-desktop-browser/report-native-light-1440.png`
- `/tmp/otto-ux-r4-insights-final/desktop-ux-r4-insights-Ins-1f3e7-coherently-native-dark-1440-desktop-browser/report-native-dark-1440.png`
- `/tmp/otto-ux-r4-insights-final/desktop-ux-r4-insights-Ins-3c23b-th-coherently-warm-dark-834-desktop-browser/report-warm-dark-834.png`
- `/tmp/otto-ux-r4-insights-final/desktop-ux-r4-insights-His-41f66-arch-across-mounted-windows-desktop-browser/history-long-keyboard.png`
- `/tmp/otto-ux-r4-insights-final/desktop-ux-r4-insights-Ass-380c5--task-cancellation-recovers-desktop-browser/assistant-cancelled.png` (shows incremental response; cancelled card above viewport).
- `/tmp/otto-ux-r4-insights-webkit/desktop-ux-r4-insights-Ass-380c5--task-cancellation-recovers-iphone-portrait/assistant-cancelled.png` (explicitly scrolled to visible Cancelled card).

Final E2E tsc and UI guards rerun after screenshot-only fixture adjustment; logs `/tmp/otto-ux-r4-insights-{tsc,guards}-final.log`, both exit0. Parent owns full UI/Rust/Clippy gates. No baseline increase or suppression.

Scores describe inspected UX and verified browser behavior (L/I/A/S/R = layout/readability, interaction, accessibility, states/recovery, responsiveness):

| Page / important variant | L | I | A | S | R | Mean |
|---|---:|---:|---:|---:|---:|---:|
| Assistant Chat: loaded desktop, phone attachments, live events/cancel | 9.5 | 9.6 | 9.5 | 9.6 | 9.5 | 9.54 |
| Assistant Tasks: complete approval preview, retry, phone/WebKit | 9.5 | 9.6 | 9.5 | 9.5 | 9.5 | 9.52 |
| Assistant Memory: all themes, both leave branches, pending save | 9.5 | 9.6 | 9.5 | 9.5 | 9.5 | 9.52 |
| History: real synthetic read/resume, 360-turn paging and keyboard search | 9.5 | 9.6 | 9.5 | 9.5 | 9.5 | 9.52 |
| Usage: scope editor, duplicates, save/refresh races, tablet RTL | 9.5 | 9.6 | 9.5 | 9.6 | 9.5 | 9.54 |
| Insights Reports: five themes, compact KPI composition, timeout/races | 9.6 | 9.6 | 9.5 | 9.6 | 9.6 | 9.58 |
| Insights Health: keyboard, dependencies, phone label, bundle fail/retry | 9.5 | 9.5 | 9.5 | 9.6 | 9.5 | 9.52 |

These scores improve where workflows were newly exercised or repaired, not because this is the final round. History keyboard search crosses the 300-turn mounted-window boundary in both directions, eliminating the specific earlier unverified reading-flow deduction. Assistant's attachment-only recovery and full failed-file identities now work on Chromium and WebKit; cancellation is an explicit confirmed synthetic task action. Reports' revised columns were visually inspected at natural desktop and 834 RTL tablet sizes.

Remaining limits: no native Tauri/physical VoiceOver or complete custom-accent contrast matrix; no production agent generation or billing enforcement. Assistant live evidence uses contract-faithful `assistant_turn` events, including same-ID updates, rather than a real provider's token-by-token PTY draft. Explicit logout plus login draft lifecycle was not newly exercised here; shared authentication review owns identity teardown. History on-disk import response is intercepted; resumed session uses isolated fake CLI. The calendar collector and manual/scheduled prompt contracts have deterministic tests, while actual agent adherence remains external-agent behavior. No known confirmed feasible defect is deferred to another round. No round5 scheduled.
