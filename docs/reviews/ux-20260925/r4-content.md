# Round 4 — Content (reviewer 3/10)

Worktree: `/Users/itziklavon/otto-ux-audit-20260925`. Scope: Product, Vault, Canvas, Design Hall, Reader, Snip. This is a scoped browser review, not a whole-app or native-Tauri signoff. Protocol, round assignment, R1/R2/R3 content reports and committed R3 parent report were read. No commits, provider-home mutations or external product writes were made. Tests used the assigned isolated daemon and synthetic fixtures. Parent owns full check/build and production-CSP integration.

## Confirmed findings and repairs

- **P1 Product rejected autosave and draft loss** — `ui/src/lib/stores/product.svelte.ts:665`: debounce settled only success, leaving a rejected PUT unhandled; returning from Alpha→Beta restored server content over the failed draft. RED logs `/tmp/otto-ux-r4-content-before3.log` (unhandled synthetic503) and `...-product-loss.log` (expected retained Alpha, received original HTML). Save scheduling now settles superseded promises, propagates failures to the caller, retains latest per-artifact drafts, cleans up the actual safe in-flight promise and prevents an older failure from replacing newer data. Explicit live reads bypass local draft restoration. `DesignArena.svelte` restores local snapshots and checks original auth generation across async conflict choices.
- **P1 Product cross-identity queued writes** — event-driven generation invalidates pending/debounced saves, old completions and draft caches. Parent independently reproduced the old queued PUT under the next login, then verified the fix with four Product identity cases across Chromium/WebKit. The parent-owned evidence is `/tmp/otto-ux-r4-parent-product-verified.log` (eight Product+Canvas cases). Source ownership stayed with this reviewer.
- **P2 Product breakpoint remount** — `ProductPage.svelte:mobileSection`: a selected artifact returned to the phone Stories list. Initialize the mobile section from the selected story. New two-artifact regression switches, resizes, induces503, receives a null-content remote event, rejects replacement, then retries and reloads both saved drafts.
- **P1 D2 never rendered in Playwright WebKit** — the installed0.1.33 worker reported ready before Go registered `self.d2`, hiding a real initialization stack overflow. A version-guarded Vite transform waits for registration and exposes startup failures. Exact WASM diagnostic maps recursion to Go regexp compilation during goldmark initialization. Latest renamed upstream0.1.34 fails the same isolated worker control; no dependency upgrade made. Exact0.1.33 runs on the main thread in both engines. `d2.ts` now falls back only for the known stack-initialization failure to a private about:blank iframe realm and MessageChannel, retaining the worker elsewhere. No parent `onmessage` or global Go/d2 mutation, no added inline scripts, no CSP relaxation. Trusted pinned package code is evaluated; diagram source remains protocol data. The fallback has bounded startup/request timeouts, removes iframe/ports on terminal failure, invalidates cache before queued work obtains an API, and preserves bfcache-suspended renderers. Ordinary syntax errors retain the live transport and prior visible SVG. Parent independently checks the production bundle under the exact desktop CSP.
- **P2 compact D2 editing/control layout** — `D2Canvas.svelte`: actual834px RTL source pane was157px; a container query stacks source and diagram when the editor is narrow, and ResizeObserver refits after layout changes. `CanvasPage.svelte` moves Ask AI above zoom controls at compact widths. Mermaid and D2 phone badges explicitly say Preview; phone Canvas is intentionally read-only, and mounted-editor identity tests use supported tablet width.
- **P2 Design Hall compact preview** — `ArtifactView.svelte:1041`: stacked320px preview/420px details left only284px of useful preview. Compact views now switch between full available preview and details, with explicit buttons and focus restoration. Detail commands and `/otto` route open the right panel. All five themes cover actual source edit and version save; phone uses the iPhone frame.
- **P2 decorative iPhone notch obscured content** — `product/design/DeviceFrame.svelte`: fresh screenshots showed the camera island over the heading. RED `/tmp/otto-ux-r4-content-notch-red.log`; added a46px content inset under the decorative island, with preview/decoration bounds assertion.
- **P2 Vault long phone tree** — `VaultPage.svelte:867`: a16-note tree consumed284px/40% of content height. Cap at `min(25%,180px)` while preserving scrolling. Regression opens the last note, reads and edits it, and verifies saved raw markdown.
- **P2 Snip keyboard drawing unavailable** — `SnipEditor.svelte:414`: canvas had no keyboard entry point. It now supports focused Enter placement, bracket selection, arrow movement, text re-edit, Delete and existing undo; focused help and selected-annotation status describe the controls. Pointer/keyboard use the same annotation/save paths. The canvas is explicitly a keyboard drawing application; narrow justified Svelte suppression accompanies its global key handler.

The earlier R3 `TypeError: Load failed` remains an untraced historical occurrence. The reproduced503 unhandled rejection is fixed, but evidence does not prove that this was the same original network event. Sandbox WebKit inspector SecurityErrors are identified separately by their actual stacks; no arbitrary Load failed filtering is used.

## Verification (updated at final handoff)

Assigned environment for every Playwright command:
`OTTO_E2E_SLOT=ux4content OTTO_E2E_PORT=7863 OTTO_E2E_PW_PORT=5363 OTTO_E2E_SWEEP_ORPHANS=0 OTTO_E2E_BIN=/Users/itziklavon/otto-ux-audit-20260925/target/debug/ottod`; one process per slot, `--workers=1`, output under `/tmp`.

- Initial independent prior regressions: `npx playwright test desktop-ux-r3-content.spec.ts desktop-ux-r3-canvas-identity.spec.ts desktop-ux-r2-content.spec.ts --project=desktop-browser --workers=1 --output=/tmp/otto-ux-r4-content-prior`: **26/26 passed**, `/tmp/otto-ux-r4-content-prior.log`.
- First comprehensive R4 run: **29/30 passed**, `/tmp/otto-ux-r4-content-verified.log`; Chromium native-light D2 overlapped an active HMR edit and lost the zoom control. No clean-run claim for that attempt. Fresh focused native-light D2 plus terminal transport tests then passed **4/4** across both engines, `/tmp/otto-ux-r4-content-transport.log`.
- Real-runtime isolated control: `/tmp/otto-ux-r4-content-d2fallback-fixed.log`. WebKit cold1283ms, second35ms, syntax-error1ms, recovered30ms, one private iframe. Chromium cold1284.5ms, second35.5ms, syntax-error0.9ms, recovered32.4ms, zero fallback iframes. These are small two-node diagrams; large graph responsiveness is not claimed.
- Final complete R4 suite / broader suites: results and parent closure are below.

## Rendered evidence actually viewed

- All five fresh R2 Reader captures `otto-ux-r2-content-browser-{native-light,native-dark,warm-light,warm-dark,pro-dark-dark}.png` from the independently rerun suite.
- Fresh R3 Design native-dark/warm-light and Vault native-dark before composition repairs; all five R3 Snip theme captures. The Snip edge text exposes a further annotation clipping issue, being repaired before handoff.
- R4 D2 before: warm-light source panel157px and clipped diagram. After: native-dark/warm-dark phone preview with clear separated Ask AI/zoom; warm-light RTL tablet and pro-dark RTL desktop restored diagrams. Captures ending `-webkit.png` preserve first successful WebKit suite, except native-light (overwritten by a concurrent Chromium HMR run and not treated as valid WebKit evidence).
- All five `otto-ux-r4-content-design-*-webkit.png` captures viewed: compact preview is useful, and phone captures revealed the notch overlap subsequently repaired.
- R4 Snip keyboard screenshot, Vault long-tree before and after, and Product failed-resize screenshot were actually viewed.
- Final engine-qualified screenshots and final score table follow after completed verification.

## Limits

No physical VoiceOver, native screenshot capture, native embedded-browser live mode, native macOS WKWebView runtime, production Jira/provider agent mutation or cloud publishing was exercised. Faithful fixtures verify the UI paths, not provider runtime completion. Canvas phone remains a preview by product design. Retained drafts remain in memory rather than across app termination. Deep D2 graphs can block the UI during the WebKit fallback; representative two-node repeat operations took30–35ms, and large-graph limits remain unmeasured. The pinned package transform must be reviewed on dependency upgrades and deliberately fails on an unexpected version or bundle shape.

## Final additional evidence

- Complete final R4 suite: `npx playwright test desktop-ux-r4-content.spec.ts --project=desktop-browser --project=ipad-portrait --workers=1 --output=/tmp/otto-ux-r4-content-final`: **32/32 passed (3.0m)**, `/tmp/otto-ux-r4-content-final.log`; all tests completed and process exited0. The actual invocation was equivalent `npm --prefix ui exec -- playwright test -c ui/playwright.config.ts …` from WT root.
- **P2 Snip committed edge text clipped from the image**: enhancing the existing loaded five-theme test with a rendered-red-pixel assertion reproduced only5px width for “Readable annotation,” `/tmp/otto-ux-r4-content-text-red.log`. `SnipEditor.svelte:351` now measures the final font/line dimensions and fits new/edited text into the source image. Actual drawing and flatten/copy share those coordinates. The existing annotation may still be deliberately moved outside by the user; text larger than the entire image is not automatically resized.
- `npx playwright test desktop-ux-r3-content.spec.ts desktop-ux-r4-content.spec.ts --grep 'Loaded Vault Design Snip|Snip creates' --project=desktop-browser --project=ipad-portrait --workers=1 --output=/tmp/otto-ux-r4-content-text-green`: **12/12 passed (2.1m)**. Fresh corrected native-dark phone and warm-light RTL Snip captures were viewed: complete text is visible. This run also rechecks Vault and Design Hall loaded themes after the notch correction.
- Final engine-qualified Design Hall native-light, native-dark phone and warm-light RTL captures were viewed at `/tmp/otto-ux-r4-content-design-<theme>-<scheme>-ipad-portrait.png`. The phone heading clears the decorative notch. Final native-light D2 source/preview capture was viewed at `/tmp/otto-ux-r4-content-d2-native-light-ipad-portrait.png`; this supersedes the contaminated earlier copy.
- Parent independently reports **10/10 production+CSP cases passed (51.0s)**, `/tmp/otto-ux-r4-d2-production-verified.log`, with a24.88s successful fresh build including persisted-pagehide handling. Parent corrected its own top-frame fixture (about:blank child origin had set API base to null) before the successful run; no product patch or CSP weakening was made for that harness failure. Parent owns this result, which is not included in this reviewer's test count.

## Scores by page and variant

L = layout/readability; I = interaction; A = accessibility; S = states/recovery; R = responsive composition. Scores describe the executed/inspected variants, not an all-feature certification. No scores are raised merely to meet the target.

| Page / variant | L | I | A | S | R | Concrete qualification |
|---|---:|---:|---:|---:|---:|---|
| Reader desktop/phone/tablet, five themes incl RTL |9.5|9.5|9.4|9.4|9.5|Fresh focus/nested-link/main-flow regressions; overlapping mark-save and new selection still deserves depth|
| Vault loaded desktop, Native light / Pro Dark RTL |9.5|9.5|9.4|9.5|9.5|File edit, search, links and persistent recovery; full tree keyboard navigation matrix not completed|
| Vault phone Native/Warm dark plus long tree; tablet Warm light RTL |9.5|9.5|9.4|9.5|9.5|Reduced tree leaves useful reading area; last-note edit persists; keyboard tree depth remains|
| Canvas Mermaid desktop and phone preview |9.5|9.5|9.4|9.5|9.5|Fresh identity/same-scene draft checks; preview/control separation and clear Preview badge; no claim of phone editing|
| Canvas D2 desktop, Native light / Pro Dark RTL |9.5|9.5|9.4|9.5|9.5|Real render/edit/error/recovery, worker/fallback and terminal retry; diagram semantics/screen-reader navigation remain limited|
| Canvas D2 phone Native/Warm dark preview |9.5|9.5|9.4|9.5|9.5|Actual WebKit render and distinct zoom/Ask AI; phone is intentionally read-only|
| Canvas D2 tablet834px Warm light RTL |9.4|9.5|9.4|9.5|9.4|Source is now usable, but fixed240px scene list still consumes39% of the614px work area; next improvement is a scene-list/detail toggle preserving draft/focus|
| Canvas Excalidraw desktop |9.4|9.5|9.3|9.5|9.4|Fresh previous round hand-draw/save-error/reopen regression; no new all-theme or full keyboard-shape editing claim|
| Design Hall desktop Native light / Pro Dark RTL |9.5|9.5|9.4|9.5|9.5|Preview/source/version save; composition stays two-pane at wide width|
| Design Hall phone Native/Warm dark and tablet Warm light RTL |9.5|9.5|9.4|9.5|9.5|Useful full-height stage, deliberate details navigation, keyboard focus restoration and camera inset; complex live agent panel sequence remains to deepen|
| Product Design Arena loaded compact desktop/phone |9.4|9.5|9.4|9.5|9.5|Two independent edits survive failure, switching, breakpoint remount and recovery; dense multi-level navigation remains a layout deduction|
| Product Discovery / synthetic Jira editing desktop |9.4|9.5|9.4|9.4|9.4|Prior late-report repair rerun; full agent-run completion/error combinations remain outside this round|
| Product phone empty/error |9.5|9.5|9.4|9.5|9.5|Prior import CTA/list retry; distinct from loaded Discovery completion|
| Snip desktop/tablet, all five themes incl RTL |9.5|9.5|9.4|9.5|9.5|Keyboard create/edit/select/move/delete/undo, readable edge text and copy path; keyboard resize/long multiline text deserve further coverage|
| Snip phone Native/Warm dark |9.4|9.5|9.4|9.5|9.5|Reachable36px controls and full annotation; multirow tool strip occupies substantial height despite useful drawing area|

Prior defects were independently rerun rather than assumed fixed. R4 specifically closes Product draft loss/rejection/identity, D2 startup/rendering, cramped source and compact Design Hall preview, Vault long-tree height, Snip keyboard creation and committed-edge-text loss. Remaining9.4/9.3 entries above are explicit remaining coverage and design limitations (round4 is final) rather than hidden in an aggregate.

- Existing desktop main workflows: `npx playwright test desktop-browser-reader.spec.ts desktop-design-hall.spec.ts desktop-vault-docs.spec.ts desktop-snip.spec.ts desktop-product-edit.spec.ts product-design-arena.spec.ts --project=desktop-browser --workers=1 --output=/tmp/otto-ux-r4-content-main`: **22/22 passed (1.9m)**, `/tmp/otto-ux-r4-content-main.log`. Desktop project matches only `desktop-*`, so the named Product Arena file was not selected; it is run separately on WebKit below. Executed Reader mark/note/session-POST fixture/save-to-Vault; Design Hall create/edit/version/compare/restore/links/references; Snip draw/copy/undo/redo/delete/missing; synthetic Jira title/description edit; Vault index/search/edit/conflict/rename/links/OpenAPI/multiple actual D2 renders/JSON/OKF.
- All five final corrected Snip theme captures were actually viewed after the pixel assertion passed. UI guards and E2E TypeScript passed; owned-file `git diff --check` passed. Logs `/tmp/otto-ux-r4-content-guards.log` and `/tmp/otto-ux-r4-content-tsc.log`. No guard baseline increase.

## Parent closure after final-round handoff

The user canceled round5. The parent finished the remaining integration checks and repaired the specific tablet scene-rail deduction: a bounded, collapsible scene list now leaves the editor its full614px width, without losing draft or focus. Both D2 and Mermaid stack source/preview at narrow available widths and refit until the user deliberately pans/zooms. Mermaid source measured257px before the repair; its regression now requires>580px and a fitted visible SVG. Native phone Canvas remains Preview. This closes that particular9.4 layout/responsive deduction; diagram keyboard/semantic coverage remains9.4. Other reviewer scores and limits above remain unchanged.

Final R4 content:17/17 WebKit checks passed in the combined22-case run (`/tmp/otto-ux-r4-parent-content-final.log`), then17/17 Chromium passed (`/tmp/otto-ux-r4-parent-content-chromium.log`, exit0). The additional five Product Arena WebKit flows passed separately after correcting double navigation in the reload fixture (`/tmp/otto-ux-r4-parent-arena.log`, exit0,1.2m). Commands ran from `ui/` with slot ux4parent, daemon7860, Vite5360, matching debug binary and one worker.

The initially failing Arena check performed page.reload followed immediately by page.goto, aborting the new document's startup fetches. Trace timing and Playwright's WebKit console-to-pageerror adapter identified the resulting browser CORS cancellation messages. Reopening the arena inside the reloaded document keeps the real reload, persisted API bytes and zero-unhandled-error assertions. No application error filter was added for Load failed, and this does not establish the cause of the older untraced R3 occurrence.

Parent viewed the final Warm-light RTL tablet Canvas screenshot: source uses full available width, diagram is fitted, scenes toggle retains keyboard focus, Ask AI and zoom are separated. The original invalid toggle icon was corrected to typed sidebar before final validation.
